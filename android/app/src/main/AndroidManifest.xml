<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Due-date reminders post through AlarmManager; the notification itself
         needs the 13+ runtime permission, requested from the settings page. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Exact alarms may be withheld by the user (Android 12+); the native code
         falls back to an inexact window when it is not granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Restores pending reminders after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Voice memos record through the microphone, with a runtime prompt. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Several launchers (ColorOS among them) refuse to place a widget unless
         the app also declares the shortcut permission, even though nothing here
         creates a shortcut. It is declared for the widget's sake. -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT"/>

    <application
        android:label="M3E Todo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to the Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues to
                 determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Fires due-date notifications. -->
        <receiver
            android:name=".DueAlarmReceiver"
            android:exported="false"/>
        <!-- Fires habit reminders, and re-arms the next one every time: a habit
             repeats, so its alarm is never simply consumed. -->
        <receiver
            android:name=".HabitAlarmReceiver"
            android:exported="false"/>
        <!-- The habit widget.

             Exported, and listening for APPWIDGET_UPDATE, because the launcher
             is a different process: it is the launcher that instantiates this
             receiver to draw the widget and to deliver taps on it, whether or
             not the app itself is running. -->
        <receiver
            android:name=".HabitWidgetProvider"
            android:exported="true"
            android:label="@string/habit_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_widget_info"/>
        </receiver>
        <!-- The course widget: today's classes. Exported for the same reason as
             the habit tile — the launcher draws it and delivers its taps. -->
        <receiver
            android:name=".CourseWidgetProvider"
            android:exported="true"
            android:label="@string/course_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/course_widget_info"/>
        </receiver>
        <!-- The question the system asks before placing a habit widget: which
             habit should this tile show. Exported because the launcher, another
             process, is what starts it. -->
        <activity
            android:name=".HabitWidgetConfigActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/habit_widget_label"
            android:theme="@style/HabitWidgetConfigTheme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>
        <!-- Re-registers pending reminders after a reboot. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <!-- Lets outside apps (gallery, video players…) read attachment files
             the app shares via ACTION_VIEW, without granting storage access to
             the whole app directory. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
