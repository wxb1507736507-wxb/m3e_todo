<?xml version="1.0" encoding="utf-8"?>
<!--
  The dialog that asks which habit a new widget should show.

  A `RemoteViews` cannot be configured from the app's own UI once it is placed,
  so the system launches this when a widget is added — and the widget is not
  placed at all until it answers. It is deliberately a plain list built in code
  from the payload the app publishes, rather than a second reader of
  `habits.json`: the app already sends every habit's emoji and name for the
  widgets to draw, and one reader of that document is enough.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <TextView
        android:id="@+id/habit_config_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="8dp"
        android:text="@string/habit_widget_config_title"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/habit_config_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:text="@string/habit_widget_config_empty"
        android:textSize="14sp"
        android:visibility="gone" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="360dp">

        <LinearLayout
            android:id="@+id/habit_config_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/habit_config_cancel"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/habit_widget_config_cancel" />
    </LinearLayout>
</LinearLayout>
