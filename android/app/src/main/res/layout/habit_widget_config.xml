<?xml version="1.0" encoding="utf-8"?>
<!--
  The arrangement screen: which habits one tile shows, and how to change them.

  Every habit is a row with a tick box. Ticking one adds it to the tile, and the
  small pencil opens that habit in the app — editing a habit's name, icon, days
  or reminder is the app's job, and duplicating that editor here would mean two
  editors to keep in step.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <TextView
        android:id="@+id/habit_config_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="4dp"
        android:text="@string/habit_widget_config_title"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/habit_config_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="8dp"
        android:text="@string/habit_widget_config_hint"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/habit_config_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:text="@string/habit_widget_config_empty"
        android:textSize="14sp"
        android:visibility="gone" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="380dp">

        <LinearLayout
            android:id="@+id/habit_config_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/habit_config_cancel"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/habit_widget_config_cancel" />

        <Button
            android:id="@+id/habit_config_done"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/habit_widget_config_done" />
    </LinearLayout>
</LinearLayout>
